"A minimal Smalltalk program demonstrating basic output.
This version shows Smalltalk's elegant message-passing paradigm,
where even string literals are fully-capable objects that can
receive messages."

'Hello, World!' printNl.
